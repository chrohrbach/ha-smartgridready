pytest>=8.0
pytest-asyncio>=0.23
PyYAML>=6.0
httpx>=0.27
# Test-only: exercises the real MILP path in src/optimizer.py. NOT part of
# smartgridready/requirements.txt (the production image) — see the comment
# there for why (armv7 wheel availability). CI runners are amd64, where
# scipy installs from a prebuilt wheel with no build-time cost.
scipy>=1.11
